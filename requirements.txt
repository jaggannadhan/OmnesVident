# Web framework & ASGI server
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
gunicorn>=22.0.0

# HTTP client
httpx>=0.27.0

# Data validation
pydantic>=2.7.0

# ORM + database
sqlmodel>=0.0.19

# RSS feed parsing
feedparser>=6.0.11

# Fuzzy string matching (deduplication)
thefuzz[speedup]>=0.22.1

# Reddit (unauthenticated; optional — falls back gracefully if absent)
asyncpraw>=7.7.0

# GCP — Firestore (optional; used in Cloud Run deployments)
google-cloud-firestore>=2.16.0

# GCP — Vertex AI / Gemini (optional; used by AIGeoRefiner in Cloud Run)
google-cloud-aiplatform[generative_ai]>=1.70.0

# OpenAI — used by AIRefiner for geo-location resolution
openai>=1.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
