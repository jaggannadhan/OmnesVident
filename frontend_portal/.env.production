# Production environment — used by Vite at build time (values are inlined).
# This file is safe to commit: it contains only the public API URL.
VITE_API_BASE_URL=https://omnesvident-api-naqkmfs2qa-uc.a.run.app
